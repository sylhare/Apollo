describe('[Subscription tests]', () => {
    test("Book Added subscription", async () => {
        // Run the server
        // Use a webclient to connect to ws://localhost:4010/subscriptions
        // make a subscriptions to bookAdded
        // Make mutation to add new book
        // Assert that BOOK_ADDED event was sent
    });
});
