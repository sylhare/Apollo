export interface ProductReference {
    id: number;
    name: string;
    description: string;
}

export interface ProductId {
    productId: number
}

export interface ProductInput {
    name: string
    description: string
}
