# Extend the type to use and add to it in multiple files without errors
extend type Query {
    allBooks: [Book]
    book(title: String!): Book
    lotr: Book
}

type Book {
    title: String!
    author: Author
}

type Author {
    name: String
}
