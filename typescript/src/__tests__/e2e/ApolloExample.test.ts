import { ApolloClient, InMemoryCache } from '@apollo/client/core';

describe("Apollo example", () => {

    // const testClient = new ApolloClient({
    //     uri: "http://localhost:3000/graphql",
    //     cache: new InMemoryCache({
    //         addTypename: true
    //     })
    // });

    test("Example call", () => {
    })
})
